/**
 * External adapters (GROQ) are implemented in a later milestone.
 * Credential name is GROQ_API_KEY — no connection is opened at build time.
 */
export const GROQ_ENV_KEY = "GROQ_API_KEY" as const;
